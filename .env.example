# Copy this file to .env and fill in real values.
# .env itself is git-ignored and should NEVER be committed.

# A long random string. Generate one with:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=change-me-to-a-real-random-value

# LDAP / Active Directory connection settings
LDAP_HOST=10.0.0.218
LDAP_PORT=636
LDAP_DOMAIN=training.local

# Added in Phase 3 (Postgres) and Phase 3's Claude-generated question bank:
# DATABASE_URL=postgresql://trainer_app:PASSWORD@localhost:5432/trainer_db
# ANTHROPIC_API_KEY=sk-ant-...
